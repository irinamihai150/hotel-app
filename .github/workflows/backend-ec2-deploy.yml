name: Build and Deploy Docker

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and push Docker image
        env:
          DOCKERHUB_USERNAME: ${ secrets.DOCKER_USERNAME}
          DOCKERHUB_TOKEN: ${ secrets.DOCKER_TOKEN }
        run: |
          echo "${DOCKER_TOKEN}" | docker login -u "${DOCKER_USERNAME}" --password-stdin
          docker build -t "${DOCKER_USERNAME}/irinamihai150/demoapp" .
          docker push "${DOCKER_USERNAME}/irinamihai150/demoapp"

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to EC2
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Pull and run Docker image on EC2
        run: |
          ssh -i ${{ secrets.SSH_PRIVATE_KEY }} ubuntu@${{ secrets.EC2_IP }} "docker pull ${{ env.DOCKER_USERNAME }}/irinamihai150/demoapp:1.0 && docker run -d --env-file .env -p 5000:5000 -e PORT=5000 ${{secrets.DOCKER_USERNAME }}/irinamihai150/demoapp:latest"
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          EC2_IP: ${{ secrets.EC2_IP }}
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}

      - name: Logout from Docker
        run: docker logout
